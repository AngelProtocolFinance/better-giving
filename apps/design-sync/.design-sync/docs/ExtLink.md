---
category: Content
---

# ExtLink

An anchor for external destinations.

Always adds `target="_blank"` and `rel="noopener noreferrer"` — those two props are intentionally not overridable. Use React Router's `Link` for internal navigation.
