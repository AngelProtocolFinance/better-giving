---
category: Feedback
---

# Prompt

A full-screen modal prompt for the outcome of an action.

`type` is `success`, `error` or `loading` and picks the icon. Its dismiss button is labelled 'Done' on success and 'Ok' otherwise — those two strings are fixed.
Use for the result of a submit; use `Modal` when you need arbitrary content.
