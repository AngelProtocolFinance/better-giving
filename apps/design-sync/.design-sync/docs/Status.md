---
category: Feedback
---

# Status

The base icon-plus-message row every status variant is built from.

Prefer the ready-made variants — `Info`, `Confirmed`, `ErrorStatus`, `LoadingStatus`. Use `Status` directly only for an icon those four do not cover.
`inline` renders it inside a text flow rather than as its own flex row.
