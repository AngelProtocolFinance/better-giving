---
category: Form
---

# Input

The bare input element with the design system's `field-input` styling.

Use when you need an input WITHOUT the label/error scaffolding — inside a custom row, a table cell, or a compound control. Reach for `Field` first; it is the normal choice.
