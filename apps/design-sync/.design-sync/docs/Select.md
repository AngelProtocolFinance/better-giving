---
category: Selector
---

# Select

A single-select dropdown built on Ark UI.

Controlled: `value`, `onChange`, `options`, and `option_disp(option)` which renders each option (so options can be objects rendered as rich rows).
Renders its own label and error paragraph. The popup only exists while open, so a static card shows the trigger.
