---
category: Feedback
---

# LoadingStatus

A spinner with a loading message.

For a pending region. For a whole-page or button-level pending state use `LoaderRing` or `LoadText`.

`StatusProps` also declares `inline` and `gap`, but this component forwards only `classes` and `children` — setting them does nothing. Use `classes` for spacing.
