---
category: Content
---

# Amount

Renders a money amount with its currency code and optional USD equivalent.

`amount` + `currency` are required. Pass `amount_usd` for a non-USD amount to show the USD equivalent alongside it.
Figures use tabular numerals so columns line up; `chips` appends badges (for example a 'Monthly' marker).
