---
category: Content
---

# Image

An image with a loading state and a `render` escape hatch for wrapping.

Set `isSrcLoading` while the source is resolving. Use `ImagePlaceholder` when there is no image at all.
