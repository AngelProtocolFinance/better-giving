// the accent pair a donation form falls back to when it stores none of its
// own — blue 9 / blue 3, so the fallback matches --primary / --secondary.
// literal because the form editor round-trips these through a color input.
export const bg_accent_primary = "#1e6dab";
export const bg_accent_secondary = "#e4f4ff";
