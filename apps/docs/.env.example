# default donation form id used across the docs playgrounds + demo page.
# client-exposed (vite inlines VITE_* into the browser bundle). optional —
# falls back to a built-in default id (see src/constants.ts).
VITE_BG_FORM_ID=E7JXrhQifQrS9XGw2CFNm
