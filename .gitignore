# custom
notes
apps/platform/scripts/settle-cheque/*.json
.claude/worktrees/

# dependencies
node_modules

# testing
coverage
.vitest-attachments
__screenshots__
codecov-report.json

# build
build
dist
*.tsbuildinfo

# sanity
.sanity

# env — real credentials only. .env.test is committed instead: it holds
# throwaway filler, nothing that opens anything. personal overrides go in
# .env.test.local, which vite loads at higher precedence.
.env
.env.local
.env.test.local
.env.staging
.env.production
.env.production.local
.env.staging

# framework
.vercel
.react-router

# turborepo
.turbo

# others
.DS_Store
/.vscode
mprocs.log

apps/platform/.server/pg/seed
apps/platform/jobs

# design-sync (staged scripts, build output, machine state)
.ds-sync/
ds-bundle/
**/.design-sync/.cache/
**/.design-sync/learnings/
**/.design-sync/node_modules
.vitest
