// the blog sanity project's identity — shared coordinates, not build output.
// hand-authored (unlike types.ts/queries.ts). platform's runtime client and
// image builder read these. the studio's sanity.config.ts and sanity.cli.ts
// carry the same values and must change with them (apps/blog/CLAUDE.md).
export const PROJECT_ID = "5820hdyj";
export const DATASET = "production";
